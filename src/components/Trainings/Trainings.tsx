import React, { useEffect } from 'react';
import SmartTable from '../../shared/SmartTable';

const Trainings = () => {
    return (
        <section>
            <SmartTable columns={[]} items={[]} />
        </section>
    );
};

export default Trainings;